# Miiix v0.2.3 Recognition Entry Self Check

Date: 2026-07-05
Codename: 识别入口

## Result

This version adds actionable recognition entry points to ingredient upload and reverse-recipe inference.

Verified build:

- `tsc && vite build`: pass
- App version shown in code: `v0.2.3 识别入口`

## Requirement Check

1. Recipe photo input
   - Status: done
   - Evidence: Recipes has a photo upload mode for inferring the cooking method from a dish image.

2. Recipe voice/text input
   - Status: done
   - Evidence: Recipes has a voice-style description mode for dish name, texture, taste, and inferred preparation.

3. Ingredient upload entrances
   - Status: done
   - Evidence: photo, online screenshot, and receipt methods now expose file upload controls before recognition.

4. Recognized ingredient review
   - Status: done
   - Evidence: recognized results are shown as image cards with status badges, confidence, selection, and ignore/restore controls.

5. Multiple recognized ingredients
   - Status: done
   - Evidence: completion saves all non-ignored recognized ingredients, while the selected item remains editable before saving.

## Product Note

v0.2.3 is an interaction prototype, not a real AI recognition release. The strict next gate is to replace mock recognition presets with OCR/vision calls, then record every user correction as a structured feedback signal.
