import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hannah.miiix',
  appName: 'Miiix',
  webDir: 'dist',
  backgroundColor: '#fbfaf6'
};

export default config;
